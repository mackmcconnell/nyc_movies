// Firestore collection structure
const schema = {
  movies: {
    title: String,
    theater: String,
    date: Timestamp,
    time: String,
    duration: String,
    director: String,
    lastUpdated: Timestamp
  }
}; 